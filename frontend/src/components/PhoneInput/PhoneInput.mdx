import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { text } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import CountryInput from './index';

<Meta title="CountryInput" component={CountryInput} />

# CountryInput

<Preview>
  <Story name="Default">
    <CountryInput
      onSetPhoneNumber={(num) => { action("onSetPhoneNumber", num) }}
      phoneNumberWithCode={text("phoneNumberWithCode", "")}
    />
  </Story>
</Preview>

## Props
```ts
interface PropShape {
  onSetPhoneNumber: (phoneNumber: string) => void;
  phoneNumberWithCode: string; // e.g. +358 44123123
}
```
<Props of={CountryInput} />
