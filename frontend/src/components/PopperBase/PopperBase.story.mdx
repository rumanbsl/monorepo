import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import PopperBase from './index';

<Meta title="PopperBase" component={PopperBase} />

# PopperBase

<Preview>
  <Story name="Default">
    <PopperBase
      activeOn="click"
      activator={<button>Activator</button>}
      component={<strong>Powered by Popper</strong>}
    />
  </Story>
  <Story name="With Arrow">
    <PopperBase
      withArrow
      activator={<p style={{border: "1px solid #f00", textAlign: "center", padding: "2rem"}} >With Arrow</p>}
      component={<em>I can be anything</em>}
    />
  </Story>
</Preview>

## Props
```ts
// Check code for clarity
interface PopperBaseProps {
  activator: React.ReactElement;
  component: React.ReactNode;
  withArrow?: boolean;
  popperColor?: keyof Variants;
}
```
<Props of={PopperBase} />
